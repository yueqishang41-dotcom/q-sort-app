# Q 分类（Q-Sort）在线实验平台项目需求文档

**研究课题：引导语框架效应对自我认知评价的影响**

- 实验类型：1×2 被试间设计（Between-subjects）
- 前端技术：Next.js / React（编译产物为静态 HTML/CSS/JavaScript）
- 后端技术：Django + SQLite
- 课程名称：实验心理学实验
- 提交日期：2026 年 8 月

---

## 一、研究背景与意义

Q 分类（Q-Sort）是 Q 方法论（Q-methodology）中的核心程序，由英国心理学家 Stephenson 于 1935 年提出。它要求被试将一组描述性陈述，按"与自己符合的程度"强制分布到若干档位上，从而以结构化、可量化的方式呈现个体的主观自我认知（Stephenson, 1935；Block, 1961）。与李克特量表等"逐题打分"方式不同，Q 分类的强制分布迫使被试在不同描述之间做出取舍，能更真实地暴露其价值结构的重心。

框架效应（framing effect）指同一信息因呈现方式（措辞、情境、暗示）不同，而导致个体判断或决策发生系统偏移的现象（Tversky & Kahneman, 1981）。在自我报告类测验中，被试对测试目的的感知以及对"社会期望答案"的推断，会显著影响其作答倾向（Edwards, 1957）；Paulhus（1984）进一步将其概念化为"印象管理"与"自我美化"两个成分，统称为社会赞许性偏差（social desirability bias）。

研究引导语对 Q 分类结果的影响具有双重意义：**理论上**，它把经典框架效应的研究范式从决策领域延伸到自我认知评价领域，检验"外部提示"能否系统性地改变被试自我描述的排序结构；**应用上**，指导语是心理测验的标准组成部分，如果引导语能在意向上偏移结果，那么测验施测时的标准化措辞就至关重要，相关发现对量表编制、人事测评与用户体验研究均有直接借鉴价值。本研究通过设计两组仅在引导语上存在差异的 Q-sort 任务，考察社会赞许性暗示是否以及在多大程度上偏移自我评价结果。

## 二、研究设计

### 2.1 实验类型

本实验采用 **1×2 被试间设计**。被试被随机分配到两个条件之一，各自只完成一种实验条件，组间不交叉。

| 设计要素 | 内容 |
|---|---|
| 设计类型 | 被试间设计（组间设计） |
| 组数 | 2 组（对照组 vs 实验组） |
| 分配方式 | JavaScript 随机分配，`Math.random()`，两组各 50% 概率 |
| 每组样本量 | 建议各 ≥ 20 人（可根据课程要求调整） |

### 2.2 自变量（操作定义）

**引导语类型（2 个水平）：**

- **对照组（control）**：呈现中性引导语——"请根据你的真实感受，将这些描述按照从'最不符合'到'最符合'的顺序排列。"
- **实验组（experimental）**：在相同引导语之前插入社会赞许性暗示句——"研究表明，心理健康水平高的人倾向于认同积极的自我描述。请根据你的真实感受，将这些描述排列。"

两组的引导语页面**布局、样式、操作流程完全一致，仅文字内容不同**，以控制无关变量。

### 2.3 因变量（操作定义）

| 编号 | 因变量 | 测量方式 |
|---|---|---|
| DV1 | **积极描述词的平均排列位置** | 计算所有 `polarity = positive` 的卡片最终所在档位的平均值（档位范围 -4 ~ +4） |
| DV2 | **排序完成总时间（秒）** | 被试点击"开始排序"到点击"提交结果"之间的时间差 |
| DV3 | **排序过程中的调整/移动次数** | 记录排序阶段内放置、交换、撤销、重做等改变卡片位置操作的累计次数 |

### 2.4 研究假设

- **H1：** 实验组被试会将积极描述词排在更靠近"最符合"（高分档）的位置，即实验组积极描述词的平均位置显著高于对照组。
  理论依据：社会赞许性偏差会诱使被试呈现更积极的自我形象（Paulhus, 1984）。
- **H2：** 实验组被试的排序完成时间显著长于对照组。
  理论依据：当"社会期望"与"真实感受"冲突时，被试需要更充分的认知加工与自我权衡，决策时间相应延长（Festinger, 1957）。
- **H3：** 实验组被试的调整/移动次数显著多于对照组。
  理论依据：社会赞许性暗示使被试在排序过程中反复犹豫、来回修改，表现为更多的调整行为。

**统计检验建议：** 三组因变量均可用独立样本 t 检验（两组间）比较组间差异；若样本分布不满足正态性假设，改用 Mann-Whitney U 检验。

## 三、实验流程

> 以下为文字版流程描述，对应网页中的页面跳转顺序（`→` 表示跳转）。

```
被试进入网站首页（/）
      │
      ▼
① 首页：查看测评说明与免责声明（知情同意）
      │   勾选"我已阅读并理解上述说明"
      ▼
② 点击"开始测评"
      │   → 系统用 Math.random() 随机分配条件（50% control / 50% experimental）
      │     条件写入本次会话，全流程保持不变，被试不可见
      ▼
③ 引导语页（/instruction）
      │   control：中性引导语
      │   experimental：带社会赞许性暗示的引导语
      │   （页面布局完全一致，仅文字不同）
      │   点击"开始排序"→ 记录开始时间戳 sortStartTime
      ▼
④ 预分堆阶段（/presort）
      │   36 张卡片逐一判断："更像我 / 不确定 / 更不像我"
      │   全部卡片分类完毕
      ▼
⑤ 精细排序阶段（/sorting）
      │   将卡片拖拽/放置到 -4 ~ +4 共 9 个档位（每档容量固定）
      │   全程记录每次移动（moveCount）与操作历史
      │   分布符合模板后点击"查看结果"
      ▼
⑥ 提交结果页（/submit）
      │   填写姓名、年龄、性别
      │   点击"提交结果" → fetch POST 到 Django 后端
      │   成功 → 绿色弹窗；失败 → 红色弹窗 + 重试按钮
      │   结束时间戳 sortEndTime = 提交时刻
      ▼
⑦ 结果页（/result）
      │   展示个人排序结果报告
      ▼
⑧ 实验结束，被试可返回首页或退出
```

## 四、系统架构

### 4.1 整体架构

```
┌─────────────────────┐         ┌──────────────────────────┐
│  前端（被试端）       │  fetch  │  后端（Django + SQLite）   │
│  Next.js / React     │  POST   │                          │
│  Tailwind CSS        │ ──────► │  views.py  submit_result │
│  部署于 Netlify      │  JSON   │  @csrf_exempt            │
│                      │         │      │                   │
│  · 随机分配条件        │         │      ▼                   │
│  · 引导语展示         │         │  QSortResult 模型 (SQLite)│
│  · Q-sort 排序交互    │         │                          │
│  · 数据打包 JSON      │         │  ┌────────────────────┐  │
└─────────────────────┘         │  │ 主试 Dashboard      │  │
                                │  │ /dashboard/ 统计图表  │  │
                                │  │ /api/export/ CSV导出  │  │
                                │  └────────────────────┘  │
                                └──────────────────────────┘
```

### 4.2 前端（被试端）

- **技术栈：** Next.js（React）+ TypeScript + Tailwind CSS + Zustand（状态管理）+ dnd-kit（拖拽交互）。
- **关键模块：**
  - `src/store/index.ts`：全局状态，`startSession()` 时调用 `assignCondition()` 随机分配实验条件；记录排序开始/结束时间戳与移动次数。
  - `src/lib/experiment.ts`：条件分配、引导语文案、实验数据打包（`buildExperimentJSON`）、提交 payload 组装（`buildSubmitPayload`）与 `fetch` 提交（`submitToBackend`）。
  - `src/app/instruction/`：引导语页（两组文字不同、布局相同）。
  - `src/app/submit/`：提交页（被试信息表单 + 提交按钮 + loading + 成功/失败弹窗 + 防重复提交）。
- **部署：** 通过 Netlify 部署（`netlify.toml` 已配置），编译产物为静态 HTML/CSS/JS。

### 4.3 后端（主试端）

- **技术栈：** Django + SQLite，已配置 `django-cors-headers`（`CORS_ALLOW_ALL_ORIGINS = True`，开发阶段放行跨域）。
- **核心接口（`experiment` 应用）：**

| 接口 | 方法 | 说明 |
|---|---|---|
| `/api/submit/` | POST | 接收被试提交的 JSON 数据，写入 `QSortResult` 表（已加 `@csrf_exempt`） |
| `/api/results/` | GET | 返回所有被试的结果数据（JSON） |
| `/api/export/` | GET | 导出全部数据为 CSV（带 BOM，Excel 中文不乱码） |
| `/dashboard/` | GET | 主试端管理页面（需登录），含统计卡片、条件筛选、平均用时柱状图 |
| `/login/`、`/logout/` | — | 主试端认证 |

### 4.4 数据流

```
前端收集（排序结果 + 时间 + 移动次数 + 被试信息）
      │
      ▼
buildSubmitPayload() 组装 JSON
      │  {
      │    "subject_name": "张三",
      │    "age": 22,
      │    "gender": "男",
      │    "condition": "experimental",
      │    "sort_data": {"card-001": 4, "card-002": -3, ...},
      │    "duration_seconds": 185,
      │    "move_count": 47,
      │    "user_agent": "Mozilla/5.0 ..."
      │  }
      ▼
fetch POST http://127.0.0.1:8000/api/submit/
      │  Content-Type: application/json
      │  CSRF 由后端 @csrf_exempt 豁免
      ▼
Django views.submit_result 解析并校验
      ▼
写入 SQLite 数据库 QSortResult 表
      ▼
主试登录 /dashboard/ 查看统计或 /api/export/ 导出 CSV
```

## 五、变量定义表

| 变量 | 类型 | 取值范围 | 测量/记录方式 |
|---|---|---|---|
| **自变量：引导语类型** `condition` | 类别变量 | `control` / `experimental` | 进入实验时 `Math.random()` 随机分配，写入会话 |
| **DV1：积极描述词平均位置** | 连续变量 | -4.00 ~ +4.00 | 所有 `polarity='positive'` 卡片最终档位分数的平均值，由 `buildExperimentJSON` 计算 |
| **DV2：排序完成时间** | 连续变量（秒） | ≥ 0 | `sortEndTime − sortStartTime`，单位换算为秒 |
| **DV3：调整/移动次数** `move_count` | 计数变量 | ≥ 0，整数 | 排序阶段每次放置/交换/撤销/重做各计 1 次 |
| 最终排序结果 `sort_data` | 对象 | cardId → 档位分数（-4~+4） | `{ "card-001": 4, "card-002": -3, ... }`，共 36 张卡片 |
| 被试姓名 `subject_name` | 文本 | 任意非空字符串 | 提交页表单输入 |
| 年龄 `age` | 数值 | 整数，选填 | 提交页表单输入 |
| 性别 `gender` | 类别变量 | 男 / 女 / 其他 / 不愿透露 | 提交页表单选择 |
| 浏览器信息 `user_agent` | 文本 | — | 前端 `navigator.userAgent`，后端亦自动采集请求头 |
| 强制分布模板 | 固定配置 | 9 档：-4~+4，容量 `[2,3,4,5,8,5,4,3,2]`，共 36 张 | `src/data/themes.ts` 中 `recommendedDistribution` |
| 卡片极性 `polarity` | 类别变量 | `positive` / `negative` / `neutral` | 36 张卡片各自的属性，用于计算 DV1 |

## 六、伦理考虑

1. **知情同意：** 被试进入实验前，首页完整展示测评说明、数据用途与免责声明（"本测评仅供自我反思与科普参考，不构成临床诊断"），须**主动勾选同意**后才能开始实验。
2. **数据匿名化：** 数据入库时由后端自动生成匿名被试编号（如 `S001`、`S002`，删除记录也不会复用编号），不直接关联被试真实身份；前端明确告知"数据仅保存在本地/以匿名形式提交"。
3. **退出自由：** 被试在实验的任何阶段（首页、引导语、排序中、提交前）均可返回或退出，未提交的数据不会被记录；退出不影响任何权利。
4. **数据保护：** 前端数据存储在浏览器本地，后端数据库仅主试通过登录访问；主试端 Dashboard 需要账号认证。
5. **无害原则：** 实验材料为日常自我描述语句，不涉及创伤性内容；结果页附专业求助提示，若被试有心理困扰建议寻求专业帮助。
6. **用途限定：** 结果仅用于课程研究用途，不作为任何临床诊断或人事决策依据。

---

## 七、交付物对照与系统层级

### 7.1 课程作业交付物对照表

| 交付物 | 状态 | 位置/说明 |
|---|---|---|
| 1. 需求文档（本文件） | ✅ | `实验项目需求文档.md` |
| 2. 高保真原型 | ✅ | `public/prototype.html`（自包含 HTML，浏览器打开或部署后访问 `/prototype.html`，含全部界面与标注） |
| 3. 完整项目代码 | ✅ | 前端 `q-sort-app/` + 后端 `qsort_backend/`，GitHub：`yueqishang41-dotcom/q-sort-app` |
| 4. 演示视频 | 📹 待录制 | 录制脚本见 `演示视频脚本.md`（1 分钟，含分镜表与常见问答） |

### 7.2 "开发者 → 主试 → 被试"三层架构

| 层级 | 入口 | 权限 | 功能 |
|---|---|---|---|
| 开发者 | `/admin/`（Django admin） | 超级用户 | 管理全部被试记录，只读字段保护（编号/提交时间） |
| 主试 | `/dashboard/`（Django 登录） | 主试账号 | 统计卡片、条件筛选、平均用时柱状图、CSV 导出 |
| 被试 | 前端各页面 | 无登录 | 完成知情同意 → 引导语 → 排序 → 提交，不可见他人数据 |

### 7.3 与评分标准的对应

- **研究设计严谨性（20）**：1×2 被试间设计，自变量操作清晰（仅引导语一句之差），三个因变量均有明确操作定义，三组假设有理论依据（见附录文献）。
- **功能实现与实验匹配（25）**：前后端已跑通验证（提交返回 `S001`、数据入库、Dashboard 可查）；三层架构完整；无"空壳逻辑"——有真实条件操控（随机分配+两版引导语）与真实数据收集。
- **创意与意义（20）**：把经典框架效应（Tversky & Kahneman, 1981）引入 Q 分类自我评价领域，对心理测验施测标准化有直接应用价值。
- **审美与平面设计（15）**：渐变配色、拖拽交互、进度反馈、成功/失败弹窗，被试体验流畅。
- **展示与讲解（15）**：演示视频按 `演示视频脚本.md` 录制，覆盖被试端完整流程与主试端数据展示，并附常见问答准备。

---

## 附录：参考理论文献（依据本文论证引用）

- Stephenson, W. (1935). *The study of behavior: Q-technique and its methodology.*
- Block, J. (1961). *The Q-Sort Method in Personality Assessment and Psychiatric Research.*
- Tversky, A., & Kahneman, D. (1981). The framing of decisions and the psychology of choice. *Science, 211*(4481), 453–458.
- Edwards, A. L. (1957). *The Social Desirability Variable in Personality Assessment and Research.*
- Paulhus, D. L. (1984). Two-component models of socially desirable responding. *Journal of Personality and Social Psychology, 46*(3), 598–609.
- Festinger, L. (1957). *A Theory of Cognitive Dissonance.*
