# 演示视频脚本（约 1 分钟）

**提示：** 用手机或电脑录屏软件（Windows 自带 `Win+G` 录屏 / OBS / 手机录屏）录制。建议先完整走一遍流程，再按本脚本正式录。**开录前先启动后端**：`cd qsort_backend && venv\Scripts\python manage.py runserver`。

---

## 分镜表

| 时间 | 画面 | 旁白（建议照读） |
|---|---|---|
| **0:00–0:05** | 打开浏览器，展示前端首页 `http://localhost:3000` | "这是一个基于 Web 的 Q-Sort 在线实验平台，用于研究**引导语框架效应对自我认知评价的影响**。采用 1×2 被试间设计。" |
| **0:05–0:12** | 勾选同意 → 点击"开始测评"，**停顿 1 秒**再点（让观众看到进入的是实验组引导语） | "被试阅读说明并知情同意后，系统会**随机分配**到对照组或实验组。这次随机到的是**实验组**——注意引导语多了一句话。" |
| **0:12–0:17** | 特写引导语页，读出实验组引导语 | "『研究表明，心理健康水平高的人倾向于认同积极的自我描述』——这就是我们操控的**社会赞许性暗示**。对照组看到的则是中性引导语，两组页面布局完全一致。" |
| **0:17–0:32** | 点击"开始排序"，快速完成预分堆（几张三堆按钮）+ 拖拽几张卡片到档位 | "被试先进行预分堆，再进入精细排序，把 36 张卡片放进 -4 到 +4 九个档位。系统全程记录**每张卡片的最终位置**、**完成时间**和**调整次数**三个因变量。" |
| **0:32–0:42** | 分布完成后点击"查看结果"，在提交页输入姓名/性别，点击"提交结果"，展示成功弹窗 | "排序完成后，被试填写基本信息并提交。我们看到数据通过 fetch POST 到 Django 后端，弹出**提交成功**，自动生成被试编号。" |
| **0:42–0:55** | 切换到主试端，浏览器打开 `http://127.0.0.1:8000/dashboard/`，登录 admin（若已登录则直接展示），点开统计数据与表格，或展示 `/api/results/` / 导出 CSV | "这是主试端 Dashboard。这里能看到被试总数、实验组与对照组的**平均完成时间对比柱状图**，以及按条件筛选的明细表，还能一键导出 CSV 做统计分析。" |
| **0:55–1:00** | 结尾：回到首页 | "完整的数据流——从被试操作到数据库再到主试端——已经跑通。以上就是本实验的被试端与主试端流程，谢谢观看。" |

---

## 录制前检查清单

- [ ] Django 后端已启动（`runserver`），否则提交会失败弹窗——这是"提交失败"最常见原因
- [ ] 前端 `npm run dev` 已启动
- [ ] 提前建好 admin 账号并登录一次（保持浏览器登录态，视频里就不用重新输密码）
- [ ] 准备演示数据：可以提前提交 2–3 条被试数据，Dashboard 柱状图更有说服力
- [ ] 如果想让观众看到"对照组 vs 实验组"对比，可录一段展示两版引导语：重开一个浏览器隐身窗口再测一次，运气好会分到对照组（若又分到实验组，可刷新重来，直到出现对照组）

## 常见问题应对（讲解环节）

**Q：为什么被试间设计而不是被试内？**
> 因为引导语一旦看过，会产生练习/污染效应，被试内设计会让第二次排序受第一次影响；被试间设计保证两组被试的排序任务完全独立。

**Q：随机分配如何实现？**
> 前端 `startSession` 时调用 `assignCondition()`，`Math.random() < 0.5` 分到 control，否则 experimental，各 50% 概率。

**Q：三个因变量怎么算？**
> DV1 积极词平均位置 = 所有 polarity=positive 卡片最终档位的平均值；DV2 = 提交时刻 − 开始排序时刻；DV3 = 排序阶段放置/交换/撤销/重做次数累计。

**Q：数据安全与伦理？**
> 被试勾选知情同意才能开始；数据入库自动生成匿名编号 S001…；被试可随时退出，未提交的数据不入库。
