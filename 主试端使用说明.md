# 主试端使用说明（老师 / 主试版）

这份说明写给查看本项目的人：**数据是怎么来的、存到哪里、如何查看**。

---

## 一、一句话总结

被试在**前端网页**完成排序并点击「提交结果」后，数据通过 API 自动存入 **Django 后端的 SQLite 数据库**。主试登录 `http://127.0.0.1:8000/dashboard/` 即可查看所有被试数据。

```
被试在网页排序 → 点「提交结果」 → fetch POST /api/submit/
   → Django 校验并写入数据库（db.sqlite3）
   → 主试登录 /dashboard/ 查看统计 / 导出 CSV
```

---

## 二、数据从哪来？（真实收集，非空壳）

本项目会**真实收集数据**，前提是有真人完成一次完整实验：

1. 被试打开前端首页，阅读说明并**勾选知情同意**；
2. 点击「开始测评」，系统**随机分配**到对照组或实验组（各 50%）；
3. 看到对应引导语 → 预分堆 → 精细排序（-4 ~ +4 共 36 张卡片）；
4. 填写姓名/年龄/性别 → 点击「提交结果」；
5. **此时**数据才通过 `POST /api/submit/` 写入数据库。

> 被试点「提交」之前，数据不会被上传。前端不自动造数据、不偷偷采集。

每份数据包含：被试编号、姓名、年龄、性别、**实验条件（control/experimental）**、**每张卡片的最终档位**、**排序用时（秒）**、**移动/调整次数**。

---

## 三、如何查看数据？（4 种方式）

### 方式 1：主试 Dashboard（推荐，统计视角）★

- 地址：**`http://127.0.0.1:8000/dashboard/`**
- 登录账号见文末「账号信息」
- 可看：被试总数、实验组/对照组人数、两组平均用时柱状图、按条件筛选的明细表
- **适合在汇报/演示时展示**

### 方式 2：Django Admin（每一条原始记录）★

- 地址：**`http://127.0.0.1:8000/admin/`**
- 登录后点「Q-sort 结果」：每一行是一条被试数据（编号/姓名/条件/用时/时间）
- 可搜索、筛选、删除、编辑

### 方式 3：原始 JSON 接口（给程序用 / 检查数据）

- 地址：**`http://127.0.0.1:8000/api/results/`**
- 返回所有记录的 JSON，含 `condition`、`sort_data`、`duration_seconds` 等

### 方式 4：导出 CSV（Excel 可打开，做统计分析）

- 地址：**`http://127.0.0.1:8000/api/export/`**
- 或命令行：
  ```
  cd qsort_backend
  venv\Scripts\python manage.py shell -c "import csv; from experiment.models import QSortResult; w=csv.writer(open('export.csv','w',newline='')); w.writerow(['subject_id','condition','duration','sort_data']); [w.writerow([r.subject_id,r.condition,r.duration_seconds,r.sort_data]) for r in QSortResult.objects.all()]"
  ```
- 导出后可直接导入 SPSS / R / Python 做独立样本 t 检验验证 H1–H3

---

## 四、账号信息（主试端登录用）

| 层级 | 地址 | 用户名 | 密码 |
|---|---|---|---|
| 主试 Dashboard / Admin | `/dashboard/`、`/admin/` | `admin` | `QsortAdmin2026!` |

> 若忘记密码，在后端目录执行：
> ```
> venv\Scripts\python manage.py changepassword admin
> ```

---

## 五、如何启动（给老师的运行说明）

```bash
# 1. 启动后端（Django + SQLite）
cd qsort_backend
venv\Scripts\python manage.py runserver      # 访问 http://127.0.0.1:8000

# 2. 启动前端（Next.js 被试端）
cd q-sort-app
npm run dev                                   # 访问 http://localhost:3000
```

> 前端页面提交的数据会发送到 `127.0.0.1:8000/api/submit/`。若后端未启动，提交会提示「提交失败」——这是预期行为，启动后端即可。
